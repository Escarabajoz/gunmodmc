// Enables automatic provisioning of the required JDK (Java 25) via the Foojay toolchain resolver.
plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'gunmod'
